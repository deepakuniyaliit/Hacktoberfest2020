#include <bits/stdc++.h>
using namespace std;
int main() 
{
	int t;
	cin >> t;
	while(t--)
	{
		int i,j,n;
		cin >> n;
		for(i=0;i<(n);i++)
	    {
			for(j=0;j<((2*n)-1);j++)
			{
				if(((i+j)>=(n-1)) && (j-i)<n)
				cout << "*";
				else
				cout << " ";
			}
			cout << "\n";
		}
	}
	return 0;
}
